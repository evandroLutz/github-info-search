import styled from "styled-components";

const ContentContainerDivisor = styled.div`
    display: flex;
    justify-content: end;
    @media (max-width: 1570px){
        flex-direction: column;
    }
`;

export default ContentContainerDivisor;